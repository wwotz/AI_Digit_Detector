# MNIST Dataset Handwritten Digit Detector

This is a project learning about AI techniques, it uses the MNIST handwritten digit 
database to train an AI to determine the digit that the user has drawn.
